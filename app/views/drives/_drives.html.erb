<div id="articles_list" class="articles_list">
	<ul>
		<% @drives.each do |drive| %>
			<a href="<%= url_for(drive) %>">
				<li class="offer_or_quest_<%= drive.offer_or_quest%> drive">
					<div class="title">
						<span>
							<% if drive.offer_or_quest == false %>
								Suche:
							<% else %>
								Fahre:
							<% end %>
						</span>
						<%= truncate(drive.title, :length => 60) %>
					</div>
					<div>
						<%= truncate(drive.description, :length => 160) %>
					</div>
					<div>
						<%= drive.user_title%> <%= truncate(drive.user_lastname, :length => 60) %> am
						<%= drive.created_at.strftime("%d.%m.%Y, %H:%M") %>
					</div>
				</li>
			</a>
		<% end %>
	</ul>
</div>

<%= will_paginate @drives, :previous_label => '&laquo; Zurück', :next_label => 'Vor &raquo;' %>